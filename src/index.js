import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // You can define global styles here if needed
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
